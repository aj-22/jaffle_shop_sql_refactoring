version: 2

seeds:
  - name: jaffle_shop_customers
    config:
      database: raw2
      schema: jaffle_shop
      alias: customers
  - name: jaffle_shop_orders
    config:
      database: raw2
      schema: jaffle_shop
      alias: orders
  - name: stripe_payments
    config:
      database: raw2
      schema: stripe
      alias: payments